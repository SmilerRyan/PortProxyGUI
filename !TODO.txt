To Do / To Fix
- When adding a new rule the comment and group are not set (seem to be empty) however they can be modified later.
- if no config ini exists by default create one
- if no rules.csv create one.
